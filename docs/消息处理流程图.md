# 消息处理流程图

本文档展示了 Telegram 频道消息监控系统的完整处理流程，包括媒体组（消息组）的处理。

## 1. 系统整体架构

```mermaid
graph TB
    A[Telegram Server] -->|UpdateNewMessage| B[TelegramClientManager]
    B -->|分发更新| C[TgUpdateNewMessageHandler]
    C -->|频道消息| D{是否为频道消息?}
    D -->|否| E[忽略]
    D -->|是| F{是否在监控列表?}
    F -->|否| G[记录日志并忽略]
    F -->|是| H[ChannelMonitorService]
    H -->|执行过滤器链| FC[FilterChainManager]
    FC -->|检查重复| FD[DuplicateMessageFilter]
    FC -->|检查空消息| FE[EmptyMessageFilter]
    FC -->|其他过滤器| FF[...]
    FC -->|过滤结果| FG{消息被接受?}
    FG -->|否| FH[丢弃消息]
    FG -->|是| I{mediaAlbumId != 0?}
    I -->|是| J[媒体组缓冲区]
    I -->|否| K[直接处理单条消息]
    J -->|定时检查超时| L[批量处理媒体组]
    K -->|保存| M[MongoDB - RawMessage]
    L -->|保存| M
    K -->|解析+插件| N[MessageParser + PluginManager]
    L -->|解析+插件| N
    N -->|输出| O[ConsolePrinterPlugin]
    
    style A fill:#e1f5ff
    style B fill:#fff4e1
    style C fill:#fff4e1
    style H fill:#e8f5e9
    style FC fill:#ffeb3b
    style FD fill:#ffeb3b
    style FE fill:#ffeb3b
    style J fill:#ffe0b2
    style M fill:#f3e5f5
    style O fill:#fce4ec
    style FH fill:#f44336,color:#fff
```

## 2. 应用启动流程

```mermaid
sequenceDiagram
    participant App as Spring Application
    participant TCM as TelegramClientManager
    participant CMS as ChannelMonitorService
    participant DB as MongoDB
    participant TG as Telegram Server

    App->>TCM: @PostConstruct 初始化
    TCM->>TCM: 验证配置
    TCM->>TG: 连接并登录
    TG-->>TCM: 登录成功
    TCM->>TCM: 注册 UpdateHandler
    
    App->>CMS: @PostConstruct 初始化
    CMS->>DB: 查询监控频道列表
    DB-->>CMS: 返回频道列表
    CMS->>CMS: 加载到内存缓存
    CMS-->>App: 初始化完成
    
    Note over App,TG: 系统就绪，开始监听消息
```

## 3. 消息接收和处理流程（含过滤器链和媒体组）

```mermaid
flowchart TD
    Start([Telegram 新消息]) --> Receive[TelegramClientManager<br/>接收 UpdateNewMessage]
    Receive --> Dispatch[分发到 Handler]
    Dispatch --> Handler[TgUpdateNewMessageHandler<br/>onNewMessageUpdate]
    
    Handler --> CheckType{检查消息类型<br/>isChannelPost?}
    CheckType -->|否| IgnoreType[忽略非频道消息]
    CheckType -->|是| CheckMonitor{检查监控列表<br/>isMonitoring?}
    
    CheckMonitor -->|否| LogIgnore[记录日志并忽略]
    CheckMonitor -->|是| Process[ChannelMonitorService<br/>handleNewMessage]
    
    Process --> FilterChain[FilterChainManager<br/>executeChain]
    FilterChain --> Filter1[DuplicateMessageFilter<br/>优先级: 95]
    Filter1 --> CheckDup{消息是否重复?}
    CheckDup -->|是| RejectDup[拒绝: 重复消息]
    CheckDup -->|否| Filter2[EmptyMessageFilter<br/>优先级: 100]
    
    Filter2 --> CheckEmpty{消息是否为空?}
    CheckEmpty -->|是| RejectEmpty[拒绝: 空消息]
    CheckEmpty -->|否| FilterN[其他过滤器...]
    
    FilterN --> FilterResult{过滤器链结果}
    FilterResult -->|拒绝| DiscardMsg[丢弃消息<br/>不保存，不处理]
    FilterResult -->|接受| CheckAlbum{检查媒体组<br/>mediaAlbumId != 0?}
    
    CheckAlbum -->|是| MediaGroup[handleMediaGroupMessage]
    CheckAlbum -->|否| Single[processSingleMessage]
    
    MediaGroup --> Buffer[添加到媒体组缓冲区]
    Buffer --> UpdateTime[更新时间戳]
    UpdateTime --> WaitMore[等待更多消息]
    
    WaitMore --> Scheduler[定时任务检查<br/>每秒执行]
    Scheduler --> CheckTimeout{超时检查<br/>2秒无新消息?}
    CheckTimeout -->|否| WaitMore
    CheckTimeout -->|是| ProcessGroup[processMediaGroup]
    
    ProcessGroup --> SortMessages[按消息ID排序]
    SortMessages --> ParseGroup[解析媒体组中的每条消息]
    
    ParseGroup --> SetGroupInfo[设置媒体组信息]
    SetGroupInfo --> SaveGroup[保存到 RawMessage 集合]
    SaveGroup --> PluginGroup[PluginManager 处理]
    PluginGroup --> PrintGroup[ConsolePrinterPlugin 输出]
    PrintGroup --> EndGroup([处理完成])
    
    Single --> Parse[MessageParser 解析]
    Parse --> Save[保存到 RawMessage 集合]
    Save --> PluginSingle[PluginManager 处理]
    PluginSingle --> Print[ConsolePrinterPlugin 输出]
    Print --> End([处理完成])
    
    IgnoreType --> End
    LogIgnore --> End
    RejectDup --> DiscardEnd([消息被丢弃])
    RejectEmpty --> DiscardEnd
    DiscardMsg --> DiscardEnd
    
    style Start fill:#e1f5ff
    style Process fill:#e8f5e9
    style FilterChain fill:#ffeb3b
    style Filter1 fill:#ffeb3b
    style Filter2 fill:#ffeb3b
    style FilterN fill:#ffeb3b
    style RejectDup fill:#f44336,color:#fff
    style RejectEmpty fill:#f44336,color:#fff
    style DiscardMsg fill:#f44336,color:#fff
    style MediaGroup fill:#ffe0b2
    style Buffer fill:#ffe0b2
    style ProcessGroup fill:#ffe0b2
    style Save fill:#f3e5f5
    style SaveGroup fill:#f3e5f5
    style Print fill:#fce4ec
    style PrintGroup fill:#fce4ec
    style End fill:#e1f5ff
    style EndGroup fill:#e1f5ff
    style DiscardEnd fill:#f44336,color:#fff
```

## 4. 过滤器链处理流程

```mermaid
flowchart TD
    Start([接收到消息]) --> FilterMgr[FilterChainManager<br/>executeChain]
    
    FilterMgr --> CreateCtx[创建 FilterContext]
    CreateCtx --> GetFilters[获取已注册过滤器列表<br/>按优先级排序]
    
    GetFilters --> CheckEmpty{过滤器列表为空?}
    CheckEmpty -->|是| AcceptDefault[默认接受消息]
    CheckEmpty -->|否| LoopStart[开始遍历过滤器]
    
    LoopStart --> NextFilter{还有过滤器?}
    NextFilter -->|否| AcceptAll[所有过滤器都接受]
    NextFilter -->|是| CheckEnabled{过滤器启用?}
    
    CheckEnabled -->|否| SkipFilter[跳过此过滤器]
    CheckEnabled -->|是| ExecuteFilter[执行过滤器]
    
    ExecuteFilter --> RecordTime[记录执行时间]
    RecordTime --> FilterLogic{过滤器逻辑}
    
    FilterLogic -->|DuplicateMessageFilter| CheckDB[检查数据库是否存在]
    FilterLogic -->|EmptyMessageFilter| CheckContent[检查内容是否为空]
    FilterLogic -->|CustomFilter| CustomLogic[自定义逻辑]
    
    CheckDB --> DBResult{消息已存在?}
    DBResult -->|是| Reject1[返回 REJECT]
    DBResult -->|否| Accept1[返回 ACCEPT]
    
    CheckContent --> ContentResult{内容为空?}
    ContentResult -->|是| Reject2[返回 REJECT]
    ContentResult -->|否| Accept2[返回 ACCEPT]
    
    CustomLogic --> CustomResult{自定义判断}
    CustomResult -->|拒绝| Reject3[返回 REJECT]
    CustomResult -->|接受| Accept3[返回 ACCEPT]
    
    Reject1 --> RecordReject[记录拒绝统计]
    Reject2 --> RecordReject
    Reject3 --> RecordReject
    
    RecordReject --> LogReject[记录拒绝日志<br/>包含原因]
    LogReject --> ReturnFalse[返回 false]
    
    Accept1 --> SkipFilter
    Accept2 --> SkipFilter
    Accept3 --> SkipFilter
    SkipFilter --> NextFilter
    
    AcceptDefault --> ReturnTrue[返回 true]
    AcceptAll --> ReturnTrue
    
    ReturnTrue --> ProcessMsg[继续处理消息<br/>保存+解析+插件]
    ReturnFalse --> DiscardMsg[丢弃消息<br/>不保存，不处理]
    
    style Start fill:#e1f5ff
    style FilterMgr fill:#ffeb3b
    style ExecuteFilter fill:#ffeb3b
    style CheckDB fill:#fff4e1
    style CheckContent fill:#fff4e1
    style CustomLogic fill:#fff4e1
    style RecordReject fill:#f44336,color:#fff
    style LogReject fill:#f44336,color:#fff
    style ReturnFalse fill:#f44336,color:#fff
    style DiscardMsg fill:#f44336,color:#fff
    style ReturnTrue fill:#4caf50,color:#fff
    style ProcessMsg fill:#4caf50,color:#fff
```

### 过滤器优先级

过滤器按优先级从高到低执行：

1. **EmptyMessageFilter (100)** - 检查空消息
2. **DuplicateMessageFilter (95)** - 检查重复消息
3. **MessageTypeFilter (90)** - 类型过滤（可选）
4. **自定义过滤器 (0-89)** - 用户自定义

### 过滤器特点

- **快速失败**: 任何过滤器返回 REJECT，立即停止链并丢弃消息
- **错误容错**: 过滤器出错时默认接受消息（fail-open）
- **统计记录**: 记录每个过滤器的执行时间和拒绝次数
- **动态管理**: 支持运行时启用/禁用过滤器

## 5. 消息内容解析详细流程

```mermaid
flowchart LR
    subgraph Input[输入]
        Msg[TdApi.Message]
    end
    
    subgraph Parse[解析过程]
        Create[创建 ChannelMessage 对象]
        Create --> Basic[设置基础字段]
        Basic --> Channel[获取频道信息]
        Channel --> Content[解析消息内容]
        
        Content --> Switch{消息类型}
        
        Switch -->|MessageText| Text[解析文本]
        Switch -->|MessagePhoto| Photo[解析图片]
        Switch -->|MessageVideo| Video[解析视频]
        Switch -->|MessageDocument| Doc[解析文档]
        Switch -->|MessageAudio| Audio[解析音频]
        Switch -->|MessageVoiceNote| Voice[解析语音]
        Switch -->|MessageVideoNote| VNote[解析视频笔记]
        Switch -->|MessageAnimation| Anim[解析动画]
        Switch -->|MessageSticker| Stick[解析贴纸]
        Switch -->|MessagePoll| Poll[解析投票]
        
        Text --> WebPage{有 WebPage?}
        WebPage -->|是| ParseWP[解析 WebPage]
        WebPage -->|否| Merge
        
        ParseWP --> InstantView{有即时预览?}
        InstantView -->|是| Telegraph[类型: telegraph]
        InstantView -->|否| Merge
        
        Photo --> ParseMedia[解析媒体文件]
        Video --> ParseMedia
        Doc --> ParseMedia
        Audio --> ParseMedia
        Voice --> ParseMedia
        VNote --> ParseMedia
        Anim --> ParseMedia
        Stick --> ParseMedia
        
        Poll --> Merge
        Telegraph --> Merge
        ParseMedia --> Merge[合并所有信息]
    end
    
    subgraph Output[输出]
        Result[ChannelMessage 对象]
    end
    
    Input --> Parse
    Parse --> Output
    
    style Input fill:#e1f5ff
    style Parse fill:#fff4e1
    style Output fill:#e8f5e9
```

## 5. Telegraph 文章识别流程

```mermaid
flowchart TD
    Start([接收文本消息]) --> Parse[解析 MessageText]
    Parse --> CheckWP{webPage 字段<br/>是否存在?}
    
    CheckWP -->|否| TextOnly[普通文本消息<br/>contentType: text]
    CheckWP -->|是| ParseWP[解析 WebPage 信息]
    
    ParseWP --> ExtractInfo[提取信息]
    ExtractInfo --> URL[url: 网页链接]
    ExtractInfo --> Title[title: 标题]
    ExtractInfo --> Author[author: 作者]
    ExtractInfo --> Desc[description: 描述]
    ExtractInfo --> Site[siteName: 网站名]
    
    URL --> CheckIV{instantViewVersion<br/>是否存在?}
    Title --> CheckIV
    Author --> CheckIV
    Desc --> CheckIV
    Site --> CheckIV
    
    CheckIV -->|否| WebPageOnly[普通网页链接<br/>contentType: text<br/>hasInstantView: false]
    CheckIV -->|是| Telegraph[Telegraph 文章<br/>contentType: telegraph<br/>hasInstantView: true]
    
    TextOnly --> Save[保存到数据库]
    WebPageOnly --> Save
    Telegraph --> Save
    
    Save --> Output[输出到控制台]
    Output --> End([完成])
    
    style Start fill:#e1f5ff
    style Telegraph fill:#4caf50,color:#fff
    style WebPageOnly fill:#ff9800,color:#fff
    style TextOnly fill:#2196f3,color:#fff
    style End fill:#e1f5ff
```

## 6. 媒体组（消息组）处理详细流程

```mermaid
flowchart TD
    Start([收到消息]) --> CheckAlbum{mediaAlbumId<br/>!= 0?}
    
    CheckAlbum -->|否| SingleMsg[单条消息处理]
    CheckAlbum -->|是| GroupKey[生成组键<br/>chatId:mediaAlbumId]
    
    GroupKey --> AddBuffer[添加到缓冲区]
    AddBuffer --> UpdateTS[更新时间戳]
    UpdateTS --> LogDebug[记录调试日志]
    LogDebug --> WaitEnd([等待更多消息])
    
    SingleMsg --> ProcessSingle[processSingleMessage]
    ProcessSingle --> EndSingle([处理完成])
    
    subgraph 定时任务 [定时任务 - 每秒执行]
        Timer[检查所有媒体组] --> FindTimeout[找出超时的组<br/>now - timestamp >= 2000ms]
        FindTimeout --> HasTimeout{有超时的组?}
        HasTimeout -->|否| TimerEnd([继续等待])
        HasTimeout -->|是| RemoveGroup[从缓冲区移除]
    end
    
    RemoveGroup --> ProcessGroup[processMediaGroup]
    ProcessGroup --> SortMsg[按消息ID排序]
    SortMsg --> CheckExists{媒体组<br/>是否已存在?}
    
    CheckExists -->|是| SkipGroup[跳过重复媒体组]
    CheckExists -->|否| CollectIds[收集所有消息ID]
    
    CollectIds --> LoopParse[循环解析每条消息]
    LoopParse --> SetGroupInfo[设置媒体组信息:<br/>mediaAlbumId<br/>isMediaGroup = true<br/>itemCount<br/>messageIds]
    
    SetGroupInfo --> SaveAll[批量保存到数据库]
    SaveAll --> PrintGroup[输出媒体组到控制台]
    PrintGroup --> GroupEnd([处理完成])
    
    SkipGroup --> GroupEnd
    
    style Start fill:#e1f5ff
    style CheckAlbum fill:#fff4e1
    style AddBuffer fill:#ffe0b2
    style Timer fill:#e8f5e9
    style ProcessGroup fill:#ffe0b2
    style SaveAll fill:#f3e5f5
    style PrintGroup fill:#fce4ec
    style GroupEnd fill:#e1f5ff
    style EndSingle fill:#e1f5ff
```

## 7. 数据库交互流程

```mermaid
sequenceDiagram
    participant CMS as ChannelMonitorService
    participant Repo as ChannelMessageRepository
    participant Mongo as MongoDB
    
    Note over CMS: 收到新消息
    
    CMS->>Repo: existsByChatIdAndMessageId(chatId, messageId)
    Repo->>Mongo: 查询唯一索引
    Mongo-->>Repo: 返回结果
    Repo-->>CMS: 是否存在
    
    alt 消息已存在
        CMS->>CMS: 记录日志并跳过
    else 消息不存在
        CMS->>CMS: 解析消息内容
        CMS->>Repo: save(channelMessage)
        Repo->>Mongo: 插入文档
        Mongo-->>Repo: 返回保存的文档
        Repo-->>CMS: 返回 ChannelMessage
        CMS->>CMS: 输出到控制台
    end
```

## 7. 数据库交互流程（含媒体组）

```mermaid
sequenceDiagram
    participant CMS as ChannelMonitorService
    participant Repo as ChannelMessageRepository
    participant Mongo as MongoDB
    
    Note over CMS: 收到新消息
    
    alt 单条消息
        CMS->>Repo: existsByChatIdAndMessageId(chatId, messageId)
        Repo->>Mongo: 查询唯一索引
        Mongo-->>Repo: 返回结果
        Repo-->>CMS: 是否存在
        
        alt 消息已存在
            CMS->>CMS: 记录日志并跳过
        else 消息不存在
            CMS->>CMS: 解析消息内容
            CMS->>Repo: save(channelMessage)
            Repo->>Mongo: 插入文档
            Mongo-->>Repo: 返回保存的文档
            Repo-->>CMS: 返回 ChannelMessage
            CMS->>CMS: 输出到控制台
        end
    else 媒体组消息
        Note over CMS: 等待媒体组完整
        CMS->>Repo: countByChatIdAndMediaAlbumId(chatId, mediaAlbumId)
        Repo->>Mongo: 查询媒体组索引
        Mongo-->>Repo: 返回数量
        Repo-->>CMS: 媒体组是否存在
        
        alt 媒体组已存在
            CMS->>CMS: 跳过重复媒体组
        else 媒体组不存在
            loop 处理每条消息
                CMS->>CMS: 解析消息并设置媒体组信息
                CMS->>Repo: save(channelMessage)
                Repo->>Mongo: 插入文档
                Mongo-->>Repo: 返回保存的文档
            end
            CMS->>CMS: 输出媒体组到控制台
        end
    end
```

## 8. 监控频道管理流程

```mermaid
stateDiagram-v2
    [*] --> 应用启动
    
    应用启动 --> 初始化监控服务: @PostConstruct
    
    初始化监控服务 --> 查询数据库: 加载监控频道
    查询数据库 --> 加载到缓存: monitoringStatus = true
    
    加载到缓存 --> 监听中: 系统就绪
    
    监听中 --> 检查消息: 收到新消息
    检查消息 --> 监听中: 不在监控列表
    检查消息 --> 处理消息: 在监控列表
    处理消息 --> 监听中: 处理完成
    
    监听中 --> 重新加载: 调用 reloadMonitoringChannels()
    重新加载 --> 查询数据库
    
    监听中 --> 添加频道: 调用 startMonitoring(chatId)
    添加频道 --> 监听中: 添加到缓存
    
    监听中 --> 移除频道: 调用 stopMonitoring(chatId)
    移除频道 --> 监听中: 从缓存移除
    
    监听中 --> 应用关闭: @PreDestroy
    应用关闭 --> [*]
```

## 8. 监控频道管理流程

```mermaid
flowchart TD
    Start([消息处理开始]) --> TryCatch{Try-Catch 块}
    
    TryCatch -->|正常| CheckExists[检查消息是否存在]
    TryCatch -->|异常| CatchError[捕获异常]
    
    CheckExists -->|已存在| LogSkip[记录跳过日志]
    CheckExists -->|不存在| ParseMsg[解析消息]
    
    ParseMsg --> TryParse{解析是否成功?}
    TryParse -->|成功| SaveDB[保存到数据库]
    TryParse -->|失败| LogParseError[记录解析错误]
    
    SaveDB --> TrySave{保存是否成功?}
    TrySave -->|成功| PrintConsole[输出到控制台]
    TrySave -->|失败| LogSaveError[记录保存错误]
    
    PrintConsole --> Success([处理成功])
    
    CatchError --> LogError[记录错误日志]
    LogError --> ErrorDetails[输出错误详情<br/>chatId, messageId]
    ErrorDetails --> Continue([继续处理下一条])
    
    LogSkip --> Continue
    LogParseError --> Continue
    LogSaveError --> Continue
    
    style Start fill:#e1f5ff
    style Success fill:#4caf50,color:#fff
    style Continue fill:#ff9800,color:#fff
    style CatchError fill:#f44336,color:#fff
    style LogError fill:#f44336,color:#fff
```

## 9. 错误处理流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant TG as Telegram Server
    participant TCM as TelegramClientManager
    participant Handler as TgUpdateNewMessageHandler
    participant CMS as ChannelMonitorService
    participant Repo as Repository
    participant DB as MongoDB
    participant Console as 控制台
    
    User->>TG: 在频道发送消息
    TG->>TCM: 推送 UpdateNewMessage
    TCM->>Handler: 分发更新事件
    
    Handler->>Handler: 检查 isChannelPost
    alt 不是频道消息
        Handler->>Handler: 忽略
    else 是频道消息
        Handler->>CMS: 检查 isMonitoring(chatId)
        
        alt 不在监控列表
            CMS-->>Handler: false
            Handler->>Console: 记录调试日志
        else 在监控列表
            CMS-->>Handler: true
            Handler->>CMS: handleNewMessage(message)
            
            CMS->>Repo: existsByChatIdAndMessageId()
            Repo->>DB: 查询
            DB-->>Repo: 结果
            Repo-->>CMS: 是否存在
            
            alt 消息已存在
                CMS->>Console: 记录跳过日志
            else 消息不存在
                CMS->>CMS: parseMessage()
                Note over CMS: 解析消息内容<br/>解析 WebPage<br/>判断 Telegraph
                
                CMS->>Repo: save(channelMessage)
                Repo->>DB: 插入文档
                DB-->>Repo: 保存成功
                Repo-->>CMS: 返回对象
                
                CMS->>Console: printMessageToConsole()
                Console->>Console: 格式化输出
                Note over Console: 显示频道信息<br/>显示消息内容<br/>显示 WebPage 信息<br/>显示媒体文件<br/>显示互动统计
            end
        end
    end
```

## 11. 完整的端到端流程（含过滤器链和媒体组）

```mermaid
graph TB
    subgraph 接收层
        A[Telegram Update] --> B[异步接收]
        B --> C[非阻塞处理]
    end
    
    subgraph 过滤层
        C --> D[频道类型过滤]
        D --> E[监控列表缓存查询]
        E --> F{在监控列表?}
        F -->|否| G[快速返回]
        F -->|是| H[过滤器链执行]
    end
    
    subgraph 过滤器链
        H --> I1[DuplicateMessageFilter<br/>检查重复]
        I1 --> I2{重复?}
        I2 -->|是| J1[拒绝消息]
        I2 -->|否| I3[EmptyMessageFilter<br/>检查空消息]
        I3 --> I4{为空?}
        I4 -->|是| J1
        I4 -->|否| I5[其他过滤器...]
        I5 --> I6{被拒绝?}
        I6 -->|是| J1
        I6 -->|否| K[继续处理]
    end
    
    subgraph 解析层
        K --> L[消息内容解析]
        L --> M[WebPage 解析]
        M --> N[媒体文件信息提取]
    end
    
    subgraph 存储层
        N --> O[保存到 RawMessage]
        O --> P[索引优化查询]
    end
    
    subgraph 插件层
        P --> Q[PluginManager 执行]
        Q --> R[ConsolePrinterPlugin]
        R --> S[其他插件...]
    end
    
    subgraph 输出层
        S --> T[异步日志输出]
        T --> U[格式化显示]
    end
    
    style A fill:#e1f5ff
    style G fill:#4caf50,color:#fff
    style H fill:#ffeb3b
    style I1 fill:#ffeb3b
    style I3 fill:#ffeb3b
    style I5 fill:#ffeb3b
    style J1 fill:#f44336,color:#fff
    style U fill:#fce4ec
```

## 11. 性能优化流程

```mermaid
stateDiagram-v2
    [*] --> 未初始化
    
    未初始化 --> 初始化中: 应用启动
    初始化中 --> 配置验证: 加载配置
    
    配置验证 --> 初始化失败: 配置错误
    配置验证 --> 连接中: 配置正确
    
    连接中 --> 登录中: TDLib 初始化完成
    登录中 --> 初始化失败: 登录失败
    登录中 --> 就绪: 登录成功
    
    就绪 --> 监听中: 加载监控频道
    
    监听中 --> 处理消息: 收到新消息
    处理消息 --> 监听中: 处理完成
    
    监听中 --> 重新加载: 配置变更
    重新加载 --> 监听中: 重新加载完成
    
    监听中 --> 关闭中: 应用关闭
    初始化失败 --> 关闭中: 清理资源
    
    关闭中 --> [*]
    
    note right of 就绪
        TelegramClientManager
        isReady() = true
    end note
    
    note right of 监听中
        ChannelMonitorService
        监控频道数量 > 0
    end note
```

## 12. 系统状态机

### 图表类型说明

1. **graph TB/LR** - 基础流程图（从上到下/从左到右）
2. **flowchart TD/LR** - 增强流程图，支持更多形状
3. **sequenceDiagram** - 时序图，展示组件间交互
4. **stateDiagram-v2** - 状态机图，展示状态转换

### 节点形状说明

- `[]` - 矩形：普通处理步骤
- `{}` - 菱形：判断/决策点
- `()` - 圆角矩形：开始/结束
- `[()]` - 圆形：状态节点

### 颜色说明

- 蓝色 (#e1f5ff) - 输入/开始
- 黄色 (#fff4e1) - 处理中
- 绿色 (#e8f5e9) - 核心服务
- 紫色 (#f3e5f5) - 数据库
- 粉色 (#fce4ec) - 输出/显示

## 使用建议

1. **查看整体架构**：从图 1 开始，了解系统组件关系
2. **理解启动流程**：查看图 2，了解系统如何初始化
3. **跟踪消息处理**：查看图 3 和图 10，了解完整的消息处理流程
4. **理解媒体组处理**：查看图 5 和图 6，了解媒体组的缓冲和批量处理机制
5. **深入特定功能**：根据需要查看 Telegraph、错误处理等专项流程图
6. **性能优化参考**：查看图 11，了解系统的性能优化点

## 媒体组（消息组）处理说明

### 什么是媒体组？

媒体组（Media Album）是 Telegram 中将多个媒体文件（图片、视频、文档、音频）作为一组发送的功能。在 TDLib 中，属于同一媒体组的消息具有相同的非零 `mediaAlbumId`。

### 处理机制

1. **缓冲机制**：
   - 收到带有 `mediaAlbumId` 的消息时，不立即处理
   - 将消息添加到缓冲区，等待同组的其他消息
   - 使用 `chatId:mediaAlbumId` 作为缓冲区的键

2. **超时机制**：
   - 每秒检查一次缓冲区
   - 如果某个媒体组 2 秒内没有新消息，认为该组已完整
   - 触发批量处理

3. **批量处理**：
   - 按消息 ID 排序，确保顺序一致
   - 检查媒体组是否已存在（去重）
   - 为每条消息设置媒体组信息
   - 批量保存到数据库
   - 统一输出到控制台

### 数据结构

```java
// ChannelMessage 实体
private Long mediaAlbumId;              // 媒体组ID，0表示不属于任何组
private Boolean isMediaGroup;           // 是否为媒体组消息
private Integer mediaGroupItemCount;    // 媒体组中的项目数量
private List<Long> mediaGroupMessageIds; // 媒体组中所有消息的ID列表
```

### 控制台输出示例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📨 收到媒体组消息
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
频道: 测试频道 (@test_channel)
媒体组ID: 5629499534213120
频道ID: -1001234567890
消息数量: 3 条
时间: 2024-02-22T20:30:45
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📎 媒体组内容:
  [1] 消息ID: 123456, 类型: photo
      说明: 第一张图片
      - 文件类型: photo, 大小: 102400 bytes
  [2] 消息ID: 123457, 类型: photo
      说明: 第二张图片
      - 文件类型: photo, 大小: 204800 bytes
  [3] 消息ID: 123458, 类型: video
      说明: 一个视频
      - 文件类型: video, 大小: 1048576 bytes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 互动: 浏览 0 次, 转发 0 次
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💾 数据库ID列表:
   - 65d7f8a9b1c2d3e4f5a6b7c8
   - 65d7f8a9b1c2d3e4f5a6b7c9
   - 65d7f8a9b1c2d3e4f5a6b7ca
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 性能考虑

1. **内存占用**：
   - 缓冲区使用 ConcurrentHashMap，线程安全
   - 超时后自动清理，避免内存泄漏
   - 通常媒体组不超过 10 条消息

2. **处理延迟**：
   - 媒体组有 2 秒的等待时间
   - 确保收集完整后再处理
   - 避免拆分显示

3. **数据库优化**：
   - 使用复合索引 `{chatId, mediaAlbumId, date}`
   - 快速查询和去重
   - 支持按媒体组查询

### 查询示例

```javascript
// 查询特定媒体组的所有消息
db.channel_messages.find({
    chatId: -1001234567890,
    mediaAlbumId: 5629499534213120
}).sort({messageId: 1})

// 统计媒体组数量
db.channel_messages.aggregate([
    {$match: {isMediaGroup: true}},
    {$group: {
        _id: {chatId: "$chatId", mediaAlbumId: "$mediaAlbumId"},
        count: {$sum: 1}
    }}
])

// 查询包含视频的媒体组
db.channel_messages.find({
    isMediaGroup: true,
    contentType: "video"
})
```

## 相关文档

- [Telegram频道实时监控技术方案](./Telegram频道实时监控技术方案.md)
- [Telegraph支持说明](./Telegraph支持说明.md)
- [功能实现总结](./功能实现总结.md)
- [Telegraph-Testing-Guide](./Telegraph-Testing-Guide.md)
- [消息过滤器链功能说明](./消息过滤器链功能说明.md)
- [过滤器快速开始](./过滤器快速开始.md)

## 过滤器链说明

### 什么是过滤器链？

过滤器链是在消息保存到数据库和交给插件处理之前执行的一系列过滤器。被过滤器拒绝的消息将被丢弃，不会保存到数据库，也不会交给插件处理。

### 为什么需要过滤器链？

1. **避免重复处理**: DuplicateMessageFilter 检查消息是否已存在，避免重复保存和处理
2. **节省存储空间**: 过滤掉无效消息（如空消息），减少数据库存储
3. **提高处理效率**: 尽早过滤掉不需要的消息，减少后续处理开销
4. **灵活的过滤规则**: 支持自定义过滤器，满足不同的业务需求

### 内置过滤器

#### 1. DuplicateMessageFilter (优先级: 95)

**功能**: 避免插件重复处理已经保存过的消息

**检查逻辑**:
- 单条消息: 使用 `chatId + messageId` 检查数据库
- 媒体组消息: 使用 `chatId + mediaAlbumId` 检查数据库

**拒绝原因示例**:
```
Duplicate message: chatId=-1001234567890, messageId=123456
Duplicate media group: chatId=-1001234567890, mediaAlbumId=789012
```

#### 2. EmptyMessageFilter (优先级: 100)

**功能**: 过滤空消息

**检查逻辑**:
- 拒绝 content 为 null 的消息
- 拒绝文本内容为空或只有空格的文本消息
- 非文本消息不检查（如图片、视频等）

**拒绝原因示例**:
```
Message content is null
Text message is empty
```

### 过滤器执行流程

```
消息接收
    ↓
过滤器链（按优先级执行）
    ↓
EmptyMessageFilter (100) → 检查空消息
    ↓
DuplicateMessageFilter (95) → 检查重复
    ↓
其他过滤器 (0-94) → 自定义逻辑
    ↓
所有过滤器都接受？
    ↓ 是
保存到数据库 → 解析消息 → 插件处理
    ↓ 否
丢弃消息（不保存，不处理）
```

### 过滤器特性

1. **优先级控制**: 数值越大优先级越高，越早执行
2. **快速失败**: 任何过滤器返回 REJECT，立即停止并丢弃消息
3. **错误容错**: 过滤器出错时默认接受消息（fail-open策略）
4. **统计记录**: 记录每个过滤器的执行时间和拒绝次数
5. **动态管理**: 支持运行时启用/禁用过滤器
6. **自动注册**: Spring Bean 自动发现和注册

### 创建自定义过滤器

```java
@Component
public class MyCustomFilter extends AbstractMessageFilter {
    
    @Override
    public String getName() {
        return "MyCustomFilter";
    }
    
    @Override
    public int getPriority() {
        return 50;  // 中等优先级
    }
    
    @Override
    protected FilterResult doFilter(TdApi.Message message, FilterContext context) {
        // 你的过滤逻辑
        if (shouldReject(message)) {
            context.setRejectReason("拒绝原因");
            return FilterResult.REJECT;
        }
        return FilterResult.ACCEPT;
    }
}
```

### 过滤器管理

```java
@Autowired
private FilterChainManager filterChainManager;

// 禁用过滤器
filterChainManager.disableFilter("MyCustomFilter");

// 启用过滤器
filterChainManager.enableFilter("MyCustomFilter");

// 获取统计
Map<String, Long> executionStats = filterChainManager.getExecutionStats();
Map<String, Long> rejectionStats = filterChainManager.getRejectionStats();
```

### 性能影响

- **单个过滤器执行时间**: < 1ms
- **过滤器链总开销**: < 5ms（假设5个过滤器）
- **内存占用**: 最小（只有过滤器实例和统计数据）

### 数据库查询优化

DuplicateMessageFilter 使用数据库索引进行快速查询：

```javascript
// 单条消息查询（使用复合唯一索引）
db.raw_messages.find({
    chatId: -1001234567890,
    messageId: 123456
}).hint({chatId: 1, messageId: 1})

// 媒体组查询（使用复合唯一索引）
db.raw_messages.find({
    chatId: -1001234567890,
    mediaAlbumId: 789012
}).hint({chatId: 1, mediaAlbumId: 1})
```

### 日志示例

```
DEBUG FilterChainManager - Filter EmptyMessageFilter executed in 0ms, result: ACCEPT
DEBUG FilterChainManager - Filter DuplicateMessageFilter executed in 2ms, result: ACCEPT
INFO  FilterChainManager - Message rejected by filter DuplicateMessageFilter: chatId=-1001234567890, messageId=123456, reason: Duplicate message: chatId=-1001234567890, messageId=123456
DEBUG ChannelMonitorService - 过滤重复消息: chatId=-1001234567890, messageId=123456
```

### 更多信息

详细的过滤器使用说明请参考：
- [消息过滤器链功能说明](./消息过滤器链功能说明.md)
- [过滤器快速开始](./过滤器快速开始.md)
